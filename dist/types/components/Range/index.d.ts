import { InputHTMLAttributes } from "react";
interface RangeProps extends InputHTMLAttributes<HTMLInputElement> {
    label: string;
}
declare const Range: (props: RangeProps) => null;
export default Range;
